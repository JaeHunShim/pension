create table reser_complete(
	reserNumber int auto_increment,
	reser_complete_date varchar(20),
    
    primary key(reserNumber)
);

drop table reser_complete;

select * from reservation;

select * from reser_complete;