<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.mapper.ReserMapper">
	<!-- 예약하는 부분  -->
	<insert id="insertReser">
		insert into reservation(reserNo,user_id,user_name,inwon_check,room_name,total_pay,r_fullDate,r_lastFullDate,reser_select,entance_time,reser_content,payment)
		values(#{reserNo},#{user_id},#{user_name},#{inwon_check},#{room_name},#{total_pay},#{r_fullDate},#{r_lastFullDate},#{reser_select},#{entance_time},#{reser_content},#{payment})
	</insert>
	<!-- 예약 정보 모두 받아오기 -->
	<select id="getReserNo" resultType="ReserVO">
		select reserNo from reservation;
	</select>
	<!-- 관리자페이지에서 볼 ㅔ약정보 불러오기 -->
	<select id="adminList" resultType="java.util.HashMap">
		select r_fullDate,r_lastfullDate,reser_select,reservation.user_id,user_email,reservation.user_name,room_name,user_phone,inwon_check,total_pay
		from user,reservation where user.user_id = reservation.user_id
		order by r_fullDate;
	</select>
</mapper>